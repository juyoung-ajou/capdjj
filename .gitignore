# .gitignore

# 1. 보안 파일 (절대 올리면 안 됨!)
.env
.env.local
backend/.env

# 2. 너무 큰 폴더 & 자동 생성 폴더 (올릴 필요 없음)
node_modules/
venv/
__pycache__/
chroma_db/
.DS_Store
dist/

# 3. 데이터 파일 (필요하면 주석 제거)
# pdf_documents/